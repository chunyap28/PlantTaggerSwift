# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Plant Tagger' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Plant Tagger
  pod 'FacebookCore'
  pod 'FacebookLogin'
  pod 'FacebookShare'
  pod 'Google/SignIn'
  pod 'Alamofire', '~> 4.2.0'
  pod 'SwiftyJSON'
  pod 'SimpleKeychain'

  target 'Plant TaggerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Plant TaggerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
